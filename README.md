# My-Components

``` bash

```

##  Multiple Dropdown with search

### Basic usage

```javascript
<Dropdown 
  multiple={true} 
  withSearch={true} 
  options={['Banna', 'Apple', 'Melon']} 
  onChange={(opt) => console.log(opt)} 
/>
```